# excel-plugin

Cross-platform spreadsheet plugins that call a Django backend:
- **Excel Add-in** (Office.js Custom Functions)
- **Google Sheets Add-on** (Apps Script), bundled to a single `Code.js`

## One-time setup

1. **Clone** and install workspace deps:
   ```bash
   npm install
