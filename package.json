{
  "name": "excel-plugin",
  "private": true,
  "workspaces": ["shared", "excel-addin", "sheets-addon"],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w excel-addin && npm run build -w sheets-addon",
    "build:shared": "npm run build -w shared",
    "dev:excel": "npm run dev -w excel-addin",
    "push:sheets": "npm run build:shared && npm run build -w sheets-addon && npm run push -w sheets-addon"
  },
  "devDependencies": { "dotenv": "^16.4.5" }
}
