{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Excel Desktop (Sample workbook)",
      "type": "pwa-msedge",
      "request": "attach",
      "useWebView": true,
      "port": 9222,
      "urlFilter": "https://localhost:3000/*",
      "timeout": 600000,
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Debug: Excel Desktop (Sample workbook)",
      "postDebugTask": "Stop Debug (Sample workbook)"
    }
  ]
}
